name: Retry due scans

on:
  schedule:
    - cron: "17 * * * *"

permissions:
  contents: read
  pages: write
  id-token: write
  actions: write

jobs:
  reconcile:
    uses: ./.github/workflows/reconcile.yml
    secrets: inherit
