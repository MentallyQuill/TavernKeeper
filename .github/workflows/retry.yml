name: Retry due scans

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: read
  issues: write
  pages: write
  id-token: write
  actions: write

jobs:
  reconcile:
    uses: ./.github/workflows/reconcile.yml
    secrets: inherit
